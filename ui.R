# =============================================================================
# WHO Growth Charts - UI shell
# The full UI is rendered server-side (build_app_ui) so it can be translated
# into the selected language at runtime.
# =============================================================================

ui <- uiOutput("app_ui")
